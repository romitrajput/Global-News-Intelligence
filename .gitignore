# Secrets — never commit the real .env; commit only .env.example
.env
.env.local
*.env

# Telegram MTProto session file (holds a live login — treat like a password)
*.session
*.session-journal

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node (video_classify.js bridge, if any tooling is added later)
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# Local test/scratch output
*.log
